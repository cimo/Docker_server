<VirtualHost *:80>
    ServerAdmin ${WWW_USER_NAME}@${DOMAIN}
    ServerName gitlab.${DOMAIN}
    ServerAlias gitlab.${DOMAIN}

    <IfModule mod_proxy.c>
        ProxyRequests off

        ProxyPass / http://127.0.0.1:8888/
        ProxyPassReverse / http://127.0.0.1:8888/
    </IfModule>

    <IfModule mod_rewrite.c>
        RewriteEngine on

        # Websocket
        RewriteCond ${HTTP:Upgrade} websocket [NC]
        RewriteCond ${HTTP:Connection} upgrade [NC]
        # Redirect to https
        RewriteRule ^(.*)$ https://%{HTTP_HOST}$1 [R=301,L]
    </IfModule>

    # Log
    LogLevel error
    ErrorLog /home/${WWW_USER_NAME}/root${WEB_PATH}gitlab_error.log
    #CustomLog /home/${WWW_USER_NAME}/root${WEB_PATH}gitlab_custom.log combined
</VirtualHost>

<VirtualHost *:443>
    ServerAdmin ${WWW_USER_NAME}@${DOMAIN}
    ServerName gitlab.${DOMAIN}
    ServerAlias gitlab.${DOMAIN}

    <IfModule mod_proxy.c>
        ProxyRequests off

        ProxyPass / https://127.0.0.1:8888/
        ProxyPassReverse / https://127.0.0.1:8888/
    </IfModule>

    <IfModule mod_ssl.c>
        SSLEngine on
        SSLProxyEngine on
        SSLProxyVerify none 
        SSLProxyCheckPeerCN off
        SSLProxyCheckPeerName off
        SSLProxyCheckPeerExpire off

        SSLCertificateFile /home/${WWW_USER_NAME}/root/certificate/${CERTIFICATE_FILE}
        SSLCertificateKeyFile /home/${WWW_USER_NAME}/root/certificate/${CERTIFICATE_KEY}
        SSLCertificateChainFile /home/${WWW_USER_NAME}/root/certificate/${CERTIFICATE_CHAIN}
    </IfModule>

    <IfModule mod_rewrite.c>
        RewriteEngine on

        # Websocket
        RewriteCond ${HTTP:Upgrade} websocket [NC]
        RewriteCond ${HTTP:Connection} upgrade [NC]
    </IfModule>

    # Log
    #LogLevel info mod_ssl.c:debug
    LogLevel error
    ErrorLog /home/${WWW_USER_NAME}/root${WEB_PATH}gitlab_error.log
    #CustomLog /home/${WWW_USER_NAME}/root${WEB_PATH}gitlab_custom.log combined
</VirtualHost>

SSLProtocol all -SSLv3 -TLSv1 -TLSv1.1
SSLCipherSuite ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384
SSLHonorCipherOrder off
SSLSessionTickets off
SSLUseStapling on
SSLStaplingCache "shmcb:logs/ssl_stapling(32768)"